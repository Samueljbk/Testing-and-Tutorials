# Create a function called area_of_rectangle that takes two arguments: width and length.
# The function should return the area of a rectangle (width * length).
# Call this function with different values for width and length and print the resulting area.


def area_of_rectangle(width: int, length: int):
    return width * length


print(area_of_rectangle(10, 5))
