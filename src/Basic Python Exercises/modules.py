# Use the Python math module to calculate the square root of a number.

import math


def get_square_root():
    number = int(input("Enter a number to find the square root of: "))
    print(math.sqrt(number))


get_square_root()
